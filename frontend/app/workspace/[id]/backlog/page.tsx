'use client';

/**
 * Backlog page — /workspace/[id]/backlog
 *
 * Renders the Backlog list view.
 */

import { BacklogPage as BacklogPageComponent } from '@/components/backlog/BacklogPage';

export default function BacklogPage() {
  return <BacklogPageComponent />;
}
