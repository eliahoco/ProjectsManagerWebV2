'use client';

/**
 * Studio page — /workspace/[id]/studio
 *
 * Renders the full Studio chat shell.
 */

import { StudioPage as StudioPageComponent } from '@/components/studio/StudioPage';

export default function StudioPage() {
  return <StudioPageComponent />;
}
